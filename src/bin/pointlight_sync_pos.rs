#![allow(non_snake_case)]
#![allow(unused)]

use wgpu_learn::scene::light::PointLight;
use wgpu_learn::scene::node::Node;
fn main() {
    // let mut node = Node::new();
    // node.set_position(2.0, 3.0, 4.0);
    // let mut p = PointLight::new(&mut node);
    // node.set_position(20.0, 3.0, 4.0);
    // node.get_position();
    // dbg!(p.camera_nodes[0].get_position());
}
